import { IconCheck } from '@shared/icons';
import styles from './IconCheck.module.scss';

export const TableIconCheck = () => {
  return <IconCheck className={styles.wrapper} width={20} height={20} />;
};
